# Phase 1 (current) does not need an API key — the AI is a local stub.
# Phase 3 adds Claude-driven factions through a local /api/agent route.
# When that lands, copy this file to `.env` and add your key:
ANTHROPIC_API_KEY=
